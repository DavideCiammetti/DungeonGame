## HOW CREATE A JAR FILE AND RUN GAME ON THE CLI OR IDE TERMINAL

- open the command prompt on the pc and run in the project directory and launch the command  ```mvn clean package```
- run the command ```java -jar target/DungeonGame-1.0-SNAPSHOT-jar-with-dependencies.jar```